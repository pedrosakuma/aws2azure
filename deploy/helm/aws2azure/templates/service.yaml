apiVersion: v1
kind: Service
metadata:
  name: {{ include "aws2azure.fullname" . }}
  labels:
    {{- include "aws2azure.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "aws2azure.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
