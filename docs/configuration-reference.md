# Complete configuration field reference

This page is generated by `tools/Aws2Azure.ConfigSchema` from the canonical
[`config.schema.json`](../config.schema.json) authoring profile and its
source-generated `ConfigDocument` contract. Do not hand-edit the field table.

A path ending in `[]` is an array item. `<name>` is an operator-selected map key.
Rows repeat where a discriminator changes a property's requirements. `None` means
the property has no schema default; omission is distinct from an empty value.

| JSON path | Type | Default | Applicability | Validation |
|---|---|---|---|---|
| `azureIdentities` | map or null | None | Named Entra identity | Optional. Named Entra identities referenced by AAD-capable backend auth blocks. |
| `azureIdentities.<name>` | object | None | Named Entra identity; `authMode=clientSecret` | Optional. Unknown properties rejected by the authoring schema. |
| `azureIdentities.<name>` | object | None | Named Entra identity; `authMode=managedIdentity` | Optional. Unknown properties rejected by the authoring schema. |
| `azureIdentities.<name>` | object | None | Named Entra identity; `authMode=workloadIdentity` | Optional. Unknown properties rejected by the authoring schema. |
| `azureIdentities.<name>.authMode` | string | `clientSecret` | Named Entra identity; `authMode=clientSecret` | Optional. Values: `clientSecret`. |
| `azureIdentities.<name>.authMode` | string | None | Named Entra identity; `authMode=managedIdentity` | Required. Values: `managedIdentity`. |
| `azureIdentities.<name>.authMode` | string | None | Named Entra identity; `authMode=workloadIdentity` | Required. Values: `workloadIdentity`. |
| `azureIdentities.<name>.clientId` | string | None | Named Entra identity; `authMode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `azureIdentities.<name>.clientId` | string or null | None | Named Entra identity; `authMode=managedIdentity` | Optional. Must contain a non-whitespace character. Minimum length 1. |
| `azureIdentities.<name>.clientSecret` | string | None | Named Entra identity; `authMode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `azureIdentities.<name>.tenantId` | string | None | Named Entra identity; `authMode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings` | array<object> | None | Each AWS identity binding | Required. At least 1 item(s). |
| `bindings[]` | object | None | Each AWS identity binding | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].aws` | object | None | Each AWS identity binding | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].aws.accessKeyId` | string | None | Each AWS identity binding | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].aws.secretAccessKey` | string | None | Each AWS identity binding | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure` | object | None | Each AWS identity binding | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.dynamodb` | object or null | None | DynamoDB / Cosmos backend | Optional. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.dynamodb.auth` | object | None | DynamoDB / Cosmos backend; `mode=clientSecret` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.dynamodb.auth` | object | None | DynamoDB / Cosmos backend; `mode=managedIdentity` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.dynamodb.auth` | object | None | DynamoDB / Cosmos backend; `mode=reference` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.dynamodb.auth` | object | None | DynamoDB / Cosmos backend; `mode=sharedKey` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.dynamodb.auth` | object | None | DynamoDB / Cosmos backend; `mode=workloadIdentity` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.dynamodb.auth.clientId` | string | None | DynamoDB / Cosmos backend; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.dynamodb.auth.clientId` | string or null | None | DynamoDB / Cosmos backend; `mode=managedIdentity` | Optional. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.dynamodb.auth.clientSecret` | string | None | DynamoDB / Cosmos backend; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.dynamodb.auth.identity` | string | None | DynamoDB / Cosmos backend; `mode=reference` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.dynamodb.auth.key` | string | None | DynamoDB / Cosmos backend; `mode=sharedKey` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.dynamodb.auth.mode` | string | None | DynamoDB / Cosmos backend; `mode=clientSecret` | Required. Values: `clientSecret`. |
| `bindings[].azure.dynamodb.auth.mode` | string | None | DynamoDB / Cosmos backend; `mode=managedIdentity` | Required. Values: `managedIdentity`. |
| `bindings[].azure.dynamodb.auth.mode` | string | None | DynamoDB / Cosmos backend; `mode=reference` | Required. Values: `reference`. |
| `bindings[].azure.dynamodb.auth.mode` | string | `sharedKey` | DynamoDB / Cosmos backend; `mode=sharedKey` | Optional. Values: `sharedKey`. |
| `bindings[].azure.dynamodb.auth.mode` | string | None | DynamoDB / Cosmos backend; `mode=workloadIdentity` | Required. Values: `workloadIdentity`. |
| `bindings[].azure.dynamodb.auth.tenantId` | string | None | DynamoDB / Cosmos backend; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.dynamodb.kind` | string | None | DynamoDB / Cosmos backend | Required. Values: `cosmos`. |
| `bindings[].azure.dynamodb.target` | object | None | DynamoDB / Cosmos backend | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.dynamodb.target.databaseName` | string | None | DynamoDB / Cosmos backend | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.dynamodb.target.endpoint` | string | None | DynamoDB / Cosmos backend | Required. Absolute URI; schemes: `http`, `https`. |
| `bindings[].azure.dynamodb.target.preferredRegions` | array<string> or null | None | DynamoDB / Cosmos backend | Optional. At least 1 item(s). |
| `bindings[].azure.dynamodb.target.preferredRegions[]` | string | None | DynamoDB / Cosmos backend | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.kinesis` | object or null | None | Kinesis / Event Hubs backend | Optional. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.kinesis.auth` | object | None | Kinesis / Event Hubs backend; `mode=clientSecret` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.kinesis.auth` | object | None | Kinesis / Event Hubs backend; `mode=managedIdentity` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.kinesis.auth` | object | None | Kinesis / Event Hubs backend; `mode=reference` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.kinesis.auth` | object | None | Kinesis / Event Hubs backend; `mode=sas` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.kinesis.auth` | object | None | Kinesis / Event Hubs backend; `mode=workloadIdentity` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.kinesis.auth.clientId` | string | None | Kinesis / Event Hubs backend; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.kinesis.auth.clientId` | string or null | None | Kinesis / Event Hubs backend; `mode=managedIdentity` | Optional. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.kinesis.auth.clientSecret` | string | None | Kinesis / Event Hubs backend; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.kinesis.auth.identity` | string | None | Kinesis / Event Hubs backend; `mode=reference` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.kinesis.auth.key` | string | None | Kinesis / Event Hubs backend; `mode=sas` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.kinesis.auth.keyName` | string | None | Kinesis / Event Hubs backend; `mode=sas` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.kinesis.auth.mode` | string | None | Kinesis / Event Hubs backend; `mode=clientSecret` | Required. Values: `clientSecret`. |
| `bindings[].azure.kinesis.auth.mode` | string | None | Kinesis / Event Hubs backend; `mode=managedIdentity` | Required. Values: `managedIdentity`. |
| `bindings[].azure.kinesis.auth.mode` | string | None | Kinesis / Event Hubs backend; `mode=reference` | Required. Values: `reference`. |
| `bindings[].azure.kinesis.auth.mode` | string | None | Kinesis / Event Hubs backend; `mode=sas` | Required. Values: `sas`. |
| `bindings[].azure.kinesis.auth.mode` | string | None | Kinesis / Event Hubs backend; `mode=workloadIdentity` | Required. Values: `workloadIdentity`. |
| `bindings[].azure.kinesis.auth.tenantId` | string | None | Kinesis / Event Hubs backend; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.kinesis.kind` | string | None | Kinesis / Event Hubs backend | Required. Values: `eventHubs`. |
| `bindings[].azure.kinesis.shardIteratorSigningKey` | string or null | None | Kinesis / Event Hubs backend | Optional. Base64-encoded HMAC key that decodes to at least 32 bytes. Minimum length 44. Encoding: base64. |
| `bindings[].azure.kinesis.streams` | map or null | None | Kinesis / Event Hubs backend | Optional. |
| `bindings[].azure.kinesis.streams.<name>` | object | None | Kinesis / Event Hubs backend | Optional. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.kinesis.streams.<name>.consumerGroup` | string or null | None | Kinesis / Event Hubs backend | Optional. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.kinesis.streams.<name>.eventHubName` | string or null | None | Kinesis / Event Hubs backend | Optional. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.kinesis.streams.<name>.partitionCount` | integer or null | None | Kinesis / Event Hubs backend | Optional. Minimum 1. |
| `bindings[].azure.kinesis.target` | object | None | Kinesis / Event Hubs backend | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.kinesis.target.endpoint` | string or null | None | Kinesis / Event Hubs backend | Optional. Absolute URI; schemes: `http`, `https`, `amqp`, `amqps`. Empty string is treated as omitted; the backend uses its configured or derived endpoint. |
| `bindings[].azure.kinesis.target.namespace` | string | None | Kinesis / Event Hubs backend | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.s3` | object or null | None | S3 / Blob backend | Optional. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.s3.auth` | object | None | S3 / Blob backend | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.s3.auth.key` | string | None | S3 / Blob backend | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.s3.auth.mode` | string | `sharedKey` | S3 / Blob backend | Optional. Values: `sharedKey`. |
| `bindings[].azure.s3.kind` | string | None | S3 / Blob backend | Required. Values: `blob`. |
| `bindings[].azure.s3.target` | object | None | S3 / Blob backend | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.s3.target.accountName` | string | None | S3 / Blob backend | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.s3.target.endpoint` | string or null | None | S3 / Blob backend | Optional. Absolute URI; schemes: `http`, `https`. Empty string is treated as omitted; the backend uses its configured or derived endpoint. |
| `bindings[].azure.secretsmanager` | object or null | None | Secrets Manager / Key Vault backend | Optional. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.secretsmanager.auth` | object | None | Secrets Manager / Key Vault backend; `mode=clientSecret` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.secretsmanager.auth` | object | None | Secrets Manager / Key Vault backend; `mode=managedIdentity` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.secretsmanager.auth` | object | None | Secrets Manager / Key Vault backend; `mode=reference` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.secretsmanager.auth` | object | None | Secrets Manager / Key Vault backend; `mode=workloadIdentity` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.secretsmanager.auth.clientId` | string | None | Secrets Manager / Key Vault backend; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.secretsmanager.auth.clientId` | string or null | None | Secrets Manager / Key Vault backend; `mode=managedIdentity` | Optional. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.secretsmanager.auth.clientSecret` | string | None | Secrets Manager / Key Vault backend; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.secretsmanager.auth.identity` | string | None | Secrets Manager / Key Vault backend; `mode=reference` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.secretsmanager.auth.mode` | string | None | Secrets Manager / Key Vault backend; `mode=clientSecret` | Required. Values: `clientSecret`. |
| `bindings[].azure.secretsmanager.auth.mode` | string | None | Secrets Manager / Key Vault backend; `mode=managedIdentity` | Required. Values: `managedIdentity`. |
| `bindings[].azure.secretsmanager.auth.mode` | string | None | Secrets Manager / Key Vault backend; `mode=reference` | Required. Values: `reference`. |
| `bindings[].azure.secretsmanager.auth.mode` | string | None | Secrets Manager / Key Vault backend; `mode=workloadIdentity` | Required. Values: `workloadIdentity`. |
| `bindings[].azure.secretsmanager.auth.tenantId` | string | None | Secrets Manager / Key Vault backend; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.secretsmanager.kind` | string | None | Secrets Manager / Key Vault backend | Required. Values: `keyVault`. |
| `bindings[].azure.secretsmanager.target` | object | None | Secrets Manager / Key Vault backend | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.secretsmanager.target.vaultUrl` | string | None | Secrets Manager / Key Vault backend | Required. Absolute URI; schemes: `https`. |
| `bindings[].azure.sns` | object or null | None | SNS / Service Bus Topics backend | Optional. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.auth` | object | None | SNS / Service Bus Topics backend; `mode=clientSecret` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.auth` | object | None | SNS / Service Bus Topics backend; `mode=managedIdentity` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.auth` | object | None | SNS / Service Bus Topics backend; `mode=reference` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.auth` | object | None | SNS / Service Bus Topics backend; `mode=sas` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.auth` | object | None | SNS / Service Bus Topics backend; `mode=workloadIdentity` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.auth.clientId` | string | None | SNS / Service Bus Topics backend; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.auth.clientId` | string or null | None | SNS / Service Bus Topics backend; `mode=managedIdentity` | Optional. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.auth.clientSecret` | string | None | SNS / Service Bus Topics backend; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.auth.identity` | string | None | SNS / Service Bus Topics backend; `mode=reference` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.auth.key` | string | None | SNS / Service Bus Topics backend; `mode=sas` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.auth.keyName` | string | None | SNS / Service Bus Topics backend; `mode=sas` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.auth.mode` | string | None | SNS / Service Bus Topics backend; `mode=clientSecret` | Required. Values: `clientSecret`. |
| `bindings[].azure.sns.auth.mode` | string | None | SNS / Service Bus Topics backend; `mode=managedIdentity` | Required. Values: `managedIdentity`. |
| `bindings[].azure.sns.auth.mode` | string | None | SNS / Service Bus Topics backend; `mode=reference` | Required. Values: `reference`. |
| `bindings[].azure.sns.auth.mode` | string | None | SNS / Service Bus Topics backend; `mode=sas` | Required. Values: `sas`. |
| `bindings[].azure.sns.auth.mode` | string | None | SNS / Service Bus Topics backend; `mode=workloadIdentity` | Required. Values: `workloadIdentity`. |
| `bindings[].azure.sns.auth.tenantId` | string | None | SNS / Service Bus Topics backend; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.eventGridFallback` | object or null | None | SNS Event Grid fallback | Optional. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.eventGridFallback.auth` | object | None | SNS Event Grid fallback; `mode=clientSecret` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.eventGridFallback.auth` | object | None | SNS Event Grid fallback; `mode=managedIdentity` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.eventGridFallback.auth` | object | None | SNS Event Grid fallback; `mode=reference` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.eventGridFallback.auth` | object | None | SNS Event Grid fallback; `mode=sharedKey` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.eventGridFallback.auth` | object | None | SNS Event Grid fallback; `mode=workloadIdentity` | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.eventGridFallback.auth.clientId` | string | None | SNS Event Grid fallback; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.eventGridFallback.auth.clientId` | string or null | None | SNS Event Grid fallback; `mode=managedIdentity` | Optional. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.eventGridFallback.auth.clientSecret` | string | None | SNS Event Grid fallback; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.eventGridFallback.auth.identity` | string | None | SNS Event Grid fallback; `mode=reference` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.eventGridFallback.auth.key` | string | None | SNS Event Grid fallback; `mode=sharedKey` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.eventGridFallback.auth.mode` | string | None | SNS Event Grid fallback; `mode=clientSecret` | Required. Values: `clientSecret`. |
| `bindings[].azure.sns.eventGridFallback.auth.mode` | string | None | SNS Event Grid fallback; `mode=managedIdentity` | Required. Values: `managedIdentity`. |
| `bindings[].azure.sns.eventGridFallback.auth.mode` | string | None | SNS Event Grid fallback; `mode=reference` | Required. Values: `reference`. |
| `bindings[].azure.sns.eventGridFallback.auth.mode` | string | `sharedKey` | SNS Event Grid fallback; `mode=sharedKey` | Optional. Values: `sharedKey`. |
| `bindings[].azure.sns.eventGridFallback.auth.mode` | string | None | SNS Event Grid fallback; `mode=workloadIdentity` | Required. Values: `workloadIdentity`. |
| `bindings[].azure.sns.eventGridFallback.auth.tenantId` | string | None | SNS Event Grid fallback; `mode=clientSecret` | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.eventGridFallback.kind` | string | None | SNS Event Grid fallback | Required. Values: `eventGrid`. |
| `bindings[].azure.sns.eventGridFallback.target` | object | None | SNS Event Grid fallback | Required. Unknown properties rejected by the authoring schema. Requires one of: `endpoint`; `namespace` + `topicName`. |
| `bindings[].azure.sns.eventGridFallback.target.endpoint` | string or null | None | SNS Event Grid fallback | Optional. Absolute URI; schemes: `https`. Empty string is treated as omitted; the backend uses its configured or derived endpoint. |
| `bindings[].azure.sns.eventGridFallback.target.namespace` | string or null | None | SNS Event Grid fallback | Optional. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.eventGridFallback.target.topicName` | string or null | None | SNS Event Grid fallback | Optional. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.kind` | string | None | SNS / Service Bus Topics backend | Required. Values: `serviceBusTopics`. |
| `bindings[].azure.sns.target` | object | None | SNS / Service Bus Topics backend | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.target.endpoint` | string or null | None | SNS / Service Bus Topics backend | Optional. Absolute URI; schemes: `http`, `https`, `amqp`, `amqps`. Empty string is treated as omitted; the backend uses its configured or derived endpoint. |
| `bindings[].azure.sns.target.managementEndpoint` | string or null | None | SNS / Service Bus Topics backend | Optional. Absolute URI; schemes: `http`, `https`. Empty string is treated as omitted; the backend uses its configured or derived endpoint. |
| `bindings[].azure.sns.target.namespace` | string | None | SNS / Service Bus Topics backend | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.topics` | map or null | None | SNS / Service Bus Topics backend | Optional. |
| `bindings[].azure.sns.topics.<name>` | object | None | SNS / Service Bus Topics backend; `backend=EventGrid` | Optional. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.topics.<name>` | object | None | SNS / Service Bus Topics backend; `backend=ServiceBusTopics` | Optional. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sns.topics.<name>.backend` | string | None | SNS / Service Bus Topics backend; `backend=EventGrid` | Required. Values: `EventGrid`. |
| `bindings[].azure.sns.topics.<name>.backend` | string | `ServiceBusTopics` | SNS / Service Bus Topics backend; `backend=ServiceBusTopics` | Optional. Values: `ServiceBusTopics`. |
| `bindings[].azure.sns.topics.<name>.eventGridAccessKey` | string or null | None | SNS / Service Bus Topics backend; `backend=EventGrid` | Optional. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sns.topics.<name>.eventGridTopicEndpoint` | string or null | None | SNS / Service Bus Topics backend; `backend=EventGrid` | Optional. Absolute URI; schemes: `https`. Empty string is treated as omitted; the backend uses its configured or derived endpoint. |
| `bindings[].azure.sns.topics.<name>.serviceBusTopicName` | string or null | None | SNS / Service Bus Topics backend; `backend=ServiceBusTopics` | Optional. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sqs` | object or null | None | SQS / Service Bus backend | Optional. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sqs.auth` | object | None | SQS / Service Bus backend | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sqs.auth.key` | string | None | SQS / Service Bus backend | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sqs.auth.keyName` | string | None | SQS / Service Bus backend | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sqs.auth.mode` | string | None | SQS / Service Bus backend | Required. Values: `sas`. |
| `bindings[].azure.sqs.kind` | string | None | SQS / Service Bus backend | Required. Values: `serviceBus`. |
| `bindings[].azure.sqs.queues` | map or null | None | SQS / Service Bus backend | Optional. |
| `bindings[].azure.sqs.queues.<name>` | object | None | SQS / Service Bus backend | Optional. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sqs.queues.<name>.transport` | string or null | None | SQS / Service Bus backend | Optional. Values: `Rest`, `Amqp`. |
| `bindings[].azure.sqs.target` | object | None | SQS / Service Bus backend | Required. Unknown properties rejected by the authoring schema. |
| `bindings[].azure.sqs.target.managementEndpoint` | string or null | None | SQS / Service Bus backend | Optional. Absolute URI; schemes: `http`, `https`. Empty string is treated as omitted; the backend uses its configured or derived endpoint. |
| `bindings[].azure.sqs.target.namespace` | string | None | SQS / Service Bus backend | Required. Must contain a non-whitespace character. Minimum length 1. |
| `bindings[].azure.sqs.target.transport` | string or null | `Rest` | SQS / Service Bus backend | Optional. Values: `Rest`, `Amqp`. |
| `services` | object | None | Configuration document | Optional. Unknown properties rejected by the authoring schema. |
| `services.dynamodb` | object or null | None | DynamoDB module | Optional. Unknown properties rejected by the authoring schema. |
| `services.dynamodb.consistencyCheck` | string | `Disabled` | DynamoDB module | Optional. Values: `Disabled`, `Warn`, `Required`. |
| `services.dynamodb.cosmosBinaryRequests` | boolean | `false` | DynamoDB module | Optional. |
| `services.dynamodb.cosmosBinaryResponses` | boolean | `false` | DynamoDB module | Optional. |
| `services.dynamodb.enableGlobalSecondaryIndexQueries` | boolean | `false` | DynamoDB module | Optional. |
| `services.dynamodb.enableLocalSecondaryIndexNumericOrdering` | boolean | `false` | DynamoDB module | Optional. |
| `services.dynamodb.enabled` | boolean | `false` | DynamoDB module | Optional. |
| `services.dynamodb.useStoredProcedures` | string | `Disabled` | DynamoDB module | Optional. Values: `Disabled`, `Preferred`, `Required`. |
| `services.kinesis` | object or null | None | Kinesis module | Optional. Unknown properties rejected by the authoring schema. |
| `services.kinesis.enabled` | boolean | `false` | Kinesis module | Optional. |
| `services.s3` | object or null | None | S3 module | Optional. Unknown properties rejected by the authoring schema. |
| `services.s3.enabled` | boolean | `false` | S3 module | Optional. |
| `services.s3.presignedTrustedSigningHosts` | array<string> or null | None | S3 module | Optional. Lowercase bare signing hosts; schemes, paths, and whitespace are not allowed. Items must be unique. |
| `services.s3.presignedTrustedSigningHosts[]` | string | None | S3 module | Required. Minimum length 1. |
| `services.secretsmanager` | object or null | None | Secrets Manager module | Optional. Unknown properties rejected by the authoring schema. |
| `services.secretsmanager.enabled` | boolean | `false` | Secrets Manager module | Optional. |
| `services.sns` | object or null | None | SNS module | Optional. Unknown properties rejected by the authoring schema. |
| `services.sns.defaultBackend` | string | `ServiceBusTopics` | SNS module | Optional. Values: `ServiceBusTopics`, `EventGrid`. |
| `services.sns.enabled` | boolean | `false` | SNS module | Optional. |
| `services.sqs` | object or null | None | SQS module | Optional. Unknown properties rejected by the authoring schema. |
| `services.sqs.enabled` | boolean | `false` | SQS module | Optional. |

## Cross-field requirements

The schema also enforces these conditions across otherwise optional fields:

- When an SNS serviceBusTopics binding omits eventGridFallback, every topic whose backend is EventGrid requires both eventGridTopicEndpoint and eventGridAccessKey.
- When services.sns.defaultBackend is EventGrid, every serviceBusTopics SNS binding requires eventGridFallback.

## Authoring profile versus v1 runtime reads

The table describes canonical authoring. New and edited files should use the exact
property names and string values shown here and pass schema validation. Runtime
reads remain a v1-compatible superset: property names are case-insensitive, unknown
extension members are ignored, defined numeric enum tokens and whitespace-padded
enum/backend values are accepted, and known fields outside the selected
backend/auth shape are ignored. Serialization always emits canonical names.

The legacy standalone SNS `kind: eventGrid` runtime shape is also outside the
authoring profile. Migrate it to `kind: serviceBusTopics` plus
`eventGridFallback`; see [Operator configuration schema](configuration-schema.md).

## Related operator references

- [Process environment variables and JSON overrides](configuration-environment.md)
- [Production configuration examples](configuration-examples.md)
- [Configuration and adoption troubleshooting](troubleshooting.md)
- [Shortest-path adoption checklist](adoption.md)
