service: s3
operation: GetBucketRequestPayment
azure_equivalent: "(no equivalent — proxy returns the S3 default body)"
status: stub
sub_features:
  - name: "configuration storage"
    status: unsupported
    notes: "Azure storage billing is account-scoped; the proxy always reports the default 'BucketOwner' payer."
behavior_differences:
  - "GET returns 200 with <RequestPaymentConfiguration> with <Payer>BucketOwner</Payer> (S3 default)."
references:
  - https://docs.aws.amazon.com/AmazonS3/latest/API/API_GetBucketRequestPayment.html
