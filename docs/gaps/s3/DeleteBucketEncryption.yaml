service: s3
operation: DeleteBucketEncryption
azure_equivalent: "Conditional container-metadata update"
status: partial
sub_features:
  - name: "idempotent AES256-intent clear"
    status: implemented
    notes: "Removes only the proxy-owned encryption-intent key and returns 204."
  - name: "Azure encryption change"
    status: unsupported
    notes: "Azure Storage encryption remains enabled and account-managed."
behavior_differences:
  - "Deleting the intent does not disable or reconfigure Azure encryption."
references:
  - https://docs.aws.amazon.com/AmazonS3/latest/API/API_DeleteBucketEncryption.html
