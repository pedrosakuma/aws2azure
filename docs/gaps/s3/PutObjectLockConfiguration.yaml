service: s3
operation: PutObjectLockConfiguration
azure_equivalent: "(bucket-level WORM is ARM/management-plane only; proxy returns 501 NotImplemented)"
status: unsupported
sub_features:
  - name: "configuration storage"
    status: unsupported
    notes: "Per-object retention/legal-hold are supported; bucket-level lock config is ARM-only. See the matching Get* gap doc."
behavior_differences:
  - "PUT returns HTTP 501 NotImplemented to make the absence explicit; the matching GET returns the documented 'never configured' shape."
references:
  - https://docs.aws.amazon.com/AmazonS3/latest/API/API_PutObjectLockConfiguration.html
