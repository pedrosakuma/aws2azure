service: s3
operation: GetPublicAccessBlock
azure_equivalent: "(no equivalent — proxy returns 404 NoSuchPublicAccessBlockConfiguration)"
status: unsupported
sub_features:
  - name: "configuration storage"
    status: unsupported
    notes: "Azure controls public access via container access level and 'allow blob public access' on the account; not bridged."
behavior_differences:
  - "GET returns HTTP 404 with code NoSuchPublicAccessBlockConfiguration so clients receive the same shape as a never-configured S3 bucket instead of InternalError."
references:
  - https://docs.aws.amazon.com/AmazonS3/latest/API/API_GetPublicAccessBlock.html
