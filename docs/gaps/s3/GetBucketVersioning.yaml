service: s3
operation: GetBucketVersioning
azure_equivalent: "(no equivalent — proxy returns an empty <VersioningConfiguration/> document)"
status: stub
sub_features:
  - name: "configuration storage"
    status: unsupported
    notes: "S3 returns an empty document when versioning has never been enabled. Azure Blob Versioning is account-scoped; the proxy does not bridge it."
behavior_differences:
  - "GET returns 200 with an empty <VersioningConfiguration/> document, matching the S3 'never configured' wire shape."
references:
  - https://docs.aws.amazon.com/AmazonS3/latest/API/API_GetBucketVersioning.html
