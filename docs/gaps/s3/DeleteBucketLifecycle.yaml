service: s3
operation: DeleteBucketLifecycle
azure_equivalent: "(no equivalent — proxy treats it as a no-op)"
status: stub
sub_features:
  - name: "idempotent clear"
    status: implemented
    note: "Returns 204 No Content because the underlying configuration is never set in the first place."
references:
  - https://docs.aws.amazon.com/AmazonS3/latest/API/API_DeleteBucketLifecycle.html
