service: s3
operation: GetBucketPolicy
azure_equivalent: "(no equivalent — proxy returns 404 NoSuchBucketPolicy)"
status: unsupported
sub_features:
  - name: "configuration storage"
    status: unsupported
    notes: "Azure has no analogue of S3 resource-based policies; access is governed by Shared Key / SAS / RBAC."
behavior_differences:
  - "GET returns HTTP 404 with code NoSuchBucketPolicy so clients receive the same shape as a never-configured S3 bucket instead of InternalError."
references:
  - https://docs.aws.amazon.com/AmazonS3/latest/API/API_GetBucketPolicy.html
