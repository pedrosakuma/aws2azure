service: dynamodb
operation: Query
azure_equivalent: "Azure Cosmos DB (Core SQL API)"
status: partial
sub_features:
  - name: KeyConditionExpression on HASH-only tables
    status: implemented
  - name: KeyConditionExpression on HASH+RANGE tables (= / < / <= / > / >= / BETWEEN / begins_with)
    status: implemented
    notes: "Translated to a partition-scoped Cosmos SQL query against `c.pk = <hash>` with a predicate on `c.id` (which holds the formatted RANGE value)."
  - name: FilterExpression
    status: implemented
    notes: "Pushed into the Cosmos SQL WHERE clause where safe; the remainder is evaluated in-process after the Cosmos page returns. ScannedCount reflects pre-filter rows and Count reflects post-filter rows, matching DynamoDB. Predicates supported: comparison (=, <, <=, >, >=), BETWEEN, IN, attribute_exists/not_exists/type, begins_with, contains, AND/OR/NOT. Pushdown carve-outs (these stay residual): `<>` on any path (DDB cross-type semantics), ordered comparisons / BETWEEN on B (base64 lexical order ≠ underlying byte order), begins_with on B, size(), nested paths whose first segment matches the reserved `_a2a:` envelope prefix. Numeric comparisons (=, BETWEEN, IN, ordered) push a hybrid IS_NUMBER / `_a2a:N` envelope branch as a *prefilter only* — the envelope branch routes through Cosmos's `StringToNumber` which rounds through a double; the client-side evaluator then re-checks the exact canonical string so high-precision values are not misclassified."
  - name: ProjectionExpression
    status: partial
    notes: "Top-level attributes and `#alias` references are honoured. Nested paths (`a.b`, `a[0]`) are not yet supported and are rejected with ValidationException."
  - name: ExpressionAttributeNames / ExpressionAttributeValues
    status: implemented
  - name: Limit
    status: implemented
  - name: ExclusiveStartKey / LastEvaluatedKey
    status: implemented
    notes: "Pagination round-trips the Cosmos `x-ms-continuation` token inside a sentinel attribute `__a2a_continuation` (typed-string `S`). Most AWS SDKs treat LastEvaluatedKey as opaque and pass it back verbatim, which is what the proxy requires."
  - name: ScanIndexForward
    status: implemented
    notes: "Maps to `ORDER BY c.id ASC|DESC`; only emitted for composite-key tables (hash-only Query returns at most one item)."
  - name: ConsistentRead
    status: implemented
    notes: "Forwards `x-ms-consistency-level: Strong` for the Cosmos query when true; account-level consistency cap still applies."
  - name: Select
    status: partial
    notes: "ALL_ATTRIBUTES (default), SPECIFIC_ATTRIBUTES, and COUNT supported. ALL_PROJECTED_ATTRIBUTES requires IndexName and is rejected."
  - name: IndexName (GSI / LSI)
    status: unsupported
    notes: "Querying secondary indexes is not yet supported; requests carrying IndexName are rejected with ValidationException."
  - name: Legacy KeyConditions / QueryFilter / ConditionalOperator
    status: unsupported
    notes: "Legacy v1 parameters are rejected loudly with ValidationException — use KeyConditionExpression / FilterExpression."
  - name: ReturnConsumedCapacity
    status: unsupported
    notes: "Silently ignored; response omits ConsumedCapacity."
behavior_differences:
  - "Sort-key ordering is the lexical order of the Cosmos document id (which is the formatted RANGE scalar string). Numeric sort keys are therefore not numerically ordered — zero-pad them or use a string sort key if order matters."
  - "Every Query is partition-scoped — there is no cross-partition fan-out — matching DynamoDB's single-partition guarantee."
  - "When a FilterExpression is pushed (fully or partially) into the Cosmos SQL, Limit's pre-filter semantics cannot be reproduced exactly — Cosmos pre-filters at the storage layer, so `scanned` becomes post-prefilter. The proxy preserves the Cosmos page boundary in that case: it returns after the first non-empty page and surfaces the Cosmos continuation as LastEvaluatedKey, rather than topping up matches across pages with a drifted page boundary."
  - "Cosmos 429 (throttled) is surfaced as DynamoDB ProvisionedThroughputExceededException."
  - "Smoke-verified against the Cosmos DB Linux emulator (vNext preview) via Testcontainers; not yet exercised against real Azure Cosmos DB."
references:
  - https://docs.aws.amazon.com/amazondynamodb/latest/APIReference/API_Query.html

