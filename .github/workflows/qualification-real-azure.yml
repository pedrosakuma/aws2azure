name: qualification-real-azure

# This workflow only evaluates immutable evidence produced by separate real-Azure
# correctness and production-shaped load runs. It never consumes emulator
# baselines or the specialized perf-real-azure A/B artifacts.
#
# mode: promote (default) qualifies a genuinely new candidate/load evidence
# built from the commit currently checked out; the correctness candidate and
# every piece of load evidence must carry that exact SHA (embedded_sha ==
# $GITHUB_SHA). mode: reaffirm renews evidence freshness for an
# already-approved *historical* runtime instead (see workload-load-real-azure.yml's
# reaffirm mode, #803/#875): the expected SHA is resolved from the profile's
# approved-runtime ledger via `export-approved-runtime`, not from the current
# checkout, and every input (correctness candidate + all load evidence) must
# carry that same historical SHA -- see #877. Note: this content-level check
# is distinct from the run-selection SHA passed to
# eng/download-qualified-run-artifact.sh -- a reaffirm load run's own GitHub
# Actions head_sha is always whatever main was on the day it ran (its
# workflow always checks out the dispatch ref), never the historical runtime
# SHA it reaffirms, so run selection for load evidence resolves and uses that
# run's actual head_sha instead of the ledger SHA.
on:
  workflow_dispatch:
    inputs:
      profile:
        description: Workload profile id
        required: true
        type: choice
        options:
          - s3-basic-object-crud
          - secretsmanager-basic-lifecycle
          - dynamodb-basic-crud
          - dynamodb-single-partition-transactions
          - sqs-standard-messaging
          - kinesis-single-consumer-per-shard
      mode:
        description: promote a new candidate/load evidence, or reaffirm already-approved historical evidence
        required: true
        default: promote
        type: choice
        options:
          - promote
          - reaffirm
      correctness_run_id:
        description: integration-real-azure run containing the correctness candidate
        required: true
        type: string
      correctness_run_attempt:
        description: Exact integration-real-azure run attempt
        required: true
        default: '1'
        type: string
      load_run_ids:
        description: Comma-separated production-shaped load run ids (never emulator or A/B runs)
        required: true
        type: string
      load_run_attempts:
        description: Comma-separated attempts aligned with load_run_ids
        required: true
        type: string
      policy_path:
        description: Reviewed repository-relative workload qualification policy
        required: true
        type: string

permissions:
  actions: read
  contents: read

jobs:
  qualify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    concurrency:
      group: qualification-real-azure-${{ inputs.profile }}
      cancel-in-progress: false
    env:
      PROFILE: ${{ inputs.profile }}
      QUALIFICATION_MODE: ${{ inputs.mode }}
      CORRECTNESS_RUN_ID: ${{ inputs.correctness_run_id }}
      CORRECTNESS_RUN_ATTEMPT: ${{ inputs.correctness_run_attempt }}
      LOAD_RUN_IDS: ${{ inputs.load_run_ids }}
      LOAD_RUN_ATTEMPTS: ${{ inputs.load_run_attempts }}
      POLICY_PATH: ${{ inputs.policy_path }}
      EVIDENCE_ROOT: artifacts/qualification-input
      PRIVATE_ROOT: artifacts/qualification-private
      OUTPUT_ROOT: artifacts/qualification-output
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4
      - uses: ./.github/actions/dotnet-setup

      - name: Build qualification tool
        run: dotnet build tools/Aws2Azure.GapDocs -c Release --nologo

      - name: Validate immutable input selection
        id: validate
        shell: bash
        env:
          REF_PROTECTED: ${{ github.ref_protected }}
        run: |
          set -euo pipefail
          if [[ "$QUALIFICATION_MODE" != promote && "$QUALIFICATION_MODE" != reaffirm ]]; then
            echo "::error::mode must be promote or reaffirm."
            exit 1
          fi
          if [[ ! "$CORRECTNESS_RUN_ID" =~ ^[1-9][0-9]*$ ]]; then
            echo "::error::correctness_run_id must be a numeric GitHub Actions run id."
            exit 1
          fi
          if [[ ! "$CORRECTNESS_RUN_ATTEMPT" =~ ^[1-9][0-9]*$ ]]; then
            echo "::error::correctness_run_attempt must be a positive integer."
            exit 1
          fi
          if [ "$REF_PROTECTED" != true ] ||
             { [ "$GITHUB_REF" != refs/heads/main ] &&
               [[ ! "$GITHUB_REF" =~ ^refs/tags/v[0-9]+\.[0-9]+\.[0-9]+-rc([.-]?[0-9A-Za-z]+)*$ ]]; }; then
            echo "::error::Qualification must run from protected main or an allowed RC tag."
            exit 1
          fi
          if [ ! -f "docs/workloads/$PROFILE.yaml" ]; then
            echo "::error::Unknown workload profile: $PROFILE"
            exit 1
          fi
          if [[ "$POLICY_PATH" = /* ]] ||
             [[ "$POLICY_PATH" == *".."* ]] ||
             [[ "$POLICY_PATH" != docs/workloads/qualification/*.yaml ]] ||
             [ ! -f "$POLICY_PATH" ]; then
            echo "::error::policy_path must name a committed file under docs/workloads/qualification/."
            exit 1
          fi
          install -d -m 0700 "$PRIVATE_ROOT" "$EVIDENCE_ROOT/selections"
          mkdir -p "$OUTPUT_ROOT"
          cp "$POLICY_PATH" "$OUTPUT_ROOT/policy.yaml"

      - name: Resolve expected candidate SHA
        id: expected_sha
        shell: bash
        run: |
          set -euo pipefail
          if [ "$QUALIFICATION_MODE" = reaffirm ]; then
            # Reaffirmation certifies the profile's already-approved historical
            # runtime, never the current checkout, so the expected SHA comes
            # from the approved-runtime ledger -- the same mechanism
            # workload-load-real-azure.yml's mode: reaffirm already uses (see
            # eng/resolve-sealed-runtime.sh and #803/#875). This also fixes the
            # single historical SHA that every downloaded artifact (correctness
            # candidate + every load-evidence run) must carry: mixing an old SHA
            # in one input with current HEAD in another cannot happen because
            # they are all checked against this one resolved value below.
            ledger="$PRIVATE_ROOT/approved-runtime-ledger.json"
            install -d -m 0700 "$PRIVATE_ROOT"
            dotnet exec tools/Aws2Azure.GapDocs/bin/Release/net10.0/Aws2Azure.GapDocs.dll \
              export-approved-runtime \
              --profile "$PROFILE" \
              --output "$ledger"
            chmod 0600 "$ledger"
            expected_sha="$(jq -er '.record.runtime.source_sha' "$ledger")"
          else
            expected_sha="$GITHUB_SHA"
          fi
          if [[ ! "$expected_sha" =~ ^[0-9a-f]{40}$ ]]; then
            echo "::error::Resolved expected SHA is not a full Git SHA."
            exit 1
          fi
          echo "expected_sha=$expected_sha" >> "$GITHUB_OUTPUT"
          echo "EXPECTED_SHA=$expected_sha" >> "$GITHUB_ENV"

      - name: Download correctness candidate
        shell: bash
        run: |
          set -euo pipefail
          destination="$PRIVATE_ROOT/correctness"
          selection="$EVIDENCE_ROOT/selections/correctness.json"
          # integration-real-azure.yml's reaffirm mode mirrors the load
          # workflow's split identity: the job still executes at its own
          # dispatch-day HEAD, but the emitted correctness candidate's content
          # (git_sha / artifact_digest / config_digest / sealed runtime
          # identity) reaffirms the historical approved runtime from the
          # ledger. Promote mode keeps the old exact equality: the run's own
          # head_sha must equal EXPECTED_SHA. Reaffirm mode instead threads the
          # correctness run's actual head_sha into artifact selection and then
          # separately checks the candidate content below against EXPECTED_SHA.
          resolve_reaffirm_run_ref() {
            local run_json="$1"
            local run_id="$2"
            local head_sha="$3"
            local branch_name
            branch_name="$(jq -er '.head_branch' "$run_json")"
            if [ "$branch_name" = main ]; then
              printf 'refs/heads/main\n'
              return
            fi
            if [[ ! "$branch_name" =~ ^v[0-9]+\.[0-9]+\.[0-9]+-rc([.-]?[0-9A-Za-z]+)*$ ]]; then
              echo "::error::Run $run_id does not use protected main or an allowed RC tag."
              exit 1
            fi
            if gh api -H "Accept: application/vnd.github+json" \
              "/repos/$GITHUB_REPOSITORY/branches/$branch_name" >/dev/null 2>&1; then
              echo "::error::Run $run_id uses an RC-shaped branch name; refusing to treat it as a protected tag."
              exit 1
            fi
            tag_ref_json="$PRIVATE_ROOT/run-$run_id-tag-ref.json"
            gh api -H "Accept: application/vnd.github+json" \
              "/repos/$GITHUB_REPOSITORY/git/ref/tags/$branch_name" > "$tag_ref_json"
            tag_object_type="$(jq -er '.object.type' "$tag_ref_json")"
            tag_object_sha="$(jq -er '.object.sha' "$tag_ref_json")"
            case "$tag_object_type" in
              commit)
                tag_commit_sha="$tag_object_sha"
                ;;
              tag)
                tag_object_json="$PRIVATE_ROOT/run-$run_id-tag-object.json"
                gh api -H "Accept: application/vnd.github+json" \
                  "/repos/$GITHUB_REPOSITORY/git/tags/$tag_object_sha" > "$tag_object_json"
                tag_commit_sha="$(jq -er '.object.sha' "$tag_object_json")"
                ;;
              *)
                echo "::error::Run $run_id tag ref resolved unexpected object type '$tag_object_type'."
                exit 1
                ;;
            esac
            if [ "$tag_commit_sha" != "$head_sha" ]; then
              echo "::error::Run $run_id RC tag does not resolve to head_sha $head_sha."
              exit 1
            fi
            printf 'refs/tags/%s\n' "$branch_name"
          }
          if [ "$QUALIFICATION_MODE" = reaffirm ]; then
            run_json="$PRIVATE_ROOT/correctness-run.json"
            gh api -H "Accept: application/vnd.github+json" \
              "/repos/$GITHUB_REPOSITORY/actions/runs/$CORRECTNESS_RUN_ID" > "$run_json"
            run_selection_sha="$(jq -er '.head_sha' "$run_json")"
            if [[ ! "$run_selection_sha" =~ ^[0-9a-f]{40}$ ]]; then
              echo "::error::Correctness run $CORRECTNESS_RUN_ID does not expose a valid head_sha."
              exit 1
            fi
            run_selection_ref="$(resolve_reaffirm_run_ref "$run_json" "$CORRECTNESS_RUN_ID" "$run_selection_sha")"
          else
            run_selection_sha="$EXPECTED_SHA"
            run_selection_ref="$GITHUB_REF"
          fi
          content="$(
            ./eng/download-qualified-run-artifact.sh \
              --repository "$GITHUB_REPOSITORY" \
              --run-id "$CORRECTNESS_RUN_ID" \
              --run-attempt "$CORRECTNESS_RUN_ATTEMPT" \
              --workflow .github/workflows/integration-real-azure.yml \
              --event workflow_dispatch \
              --profile "$PROFILE" \
              --expected-sha "$run_selection_sha" \
              --expected-ref "$run_selection_ref" \
              --artifact-name real-azure-conformance \
              --destination "$destination" \
              --identity-output "$selection"
          )"
          mapfile -t candidates < <(
            find "$content" -type f -path "*/candidates/$PROFILE.yaml" | sort
          )
          if [ "${#candidates[@]}" -ne 1 ]; then
            echo "::error::Correctness run must contain exactly one candidate $PROFILE."
            exit 1
          fi
          candidate="${candidates[0]}"
          embedded_run_id="$(sed -n 's/^  run_id: //p' "$candidate" | head -n 1 | tr -d '\"')"
          if [ "$embedded_run_id" != "$CORRECTNESS_RUN_ID" ]; then
            echo "::error::Correctness candidate provenance run_id '$embedded_run_id' does not match selected run $CORRECTNESS_RUN_ID."
            exit 1
          fi
          embedded_attempt="$(sed -n 's/^  run_attempt: //p' "$candidate" | head -n 1 | tr -d '\"')"
          if [ "$embedded_attempt" != "$CORRECTNESS_RUN_ATTEMPT" ]; then
            echo "::error::Correctness candidate attempt '$embedded_attempt' does not match selected attempt."
            exit 1
          fi
          embedded_sha="$(sed -n 's/^  git_sha: //p' "$candidate" | head -n 1 | tr -d '\"')"
          if [ "$embedded_sha" != "$EXPECTED_SHA" ]; then
            echo "::error::Correctness candidate SHA '$embedded_sha' does not match the expected $QUALIFICATION_MODE SHA '$EXPECTED_SHA'."
            exit 1
          fi
          printf '%s\n' "$candidate" > "$EVIDENCE_ROOT/correctness-candidate-path.txt"

      - name: Download repeated production-shaped load evidence
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$EVIDENCE_ROOT/load"
          evidence_paths="$EVIDENCE_ROOT/load-evidence-paths.txt"
          selection_paths="$EVIDENCE_ROOT/load-selection-paths.txt"
          : > "$evidence_paths"
          : > "$selection_paths"
          IFS=',' read -ra run_ids <<< "$LOAD_RUN_IDS"
          IFS=',' read -ra run_attempts <<< "$LOAD_RUN_ATTEMPTS"
          if [ "${#run_ids[@]}" -ne "${#run_attempts[@]}" ]; then
            echo "::error::load_run_attempts must align one-to-one with load_run_ids."
            exit 1
          fi
          seen=()
          resolve_reaffirm_run_ref() {
            local run_json="$1"
            local run_id="$2"
            local head_sha="$3"
            local branch_name
            branch_name="$(jq -er '.head_branch' "$run_json")"
            if [ "$branch_name" = main ]; then
              printf 'refs/heads/main\n'
              return
            fi
            if [[ ! "$branch_name" =~ ^v[0-9]+\.[0-9]+\.[0-9]+-rc([.-]?[0-9A-Za-z]+)*$ ]]; then
              echo "::error::Run $run_id does not use protected main or an allowed RC tag."
              exit 1
            fi
            if gh api -H "Accept: application/vnd.github+json" \
              "/repos/$GITHUB_REPOSITORY/branches/$branch_name" >/dev/null 2>&1; then
              echo "::error::Run $run_id uses an RC-shaped branch name; refusing to treat it as a protected tag."
              exit 1
            fi
            tag_ref_json="$PRIVATE_ROOT/run-$run_id-tag-ref.json"
            gh api -H "Accept: application/vnd.github+json" \
              "/repos/$GITHUB_REPOSITORY/git/ref/tags/$branch_name" > "$tag_ref_json"
            tag_object_type="$(jq -er '.object.type' "$tag_ref_json")"
            tag_object_sha="$(jq -er '.object.sha' "$tag_ref_json")"
            case "$tag_object_type" in
              commit)
                tag_commit_sha="$tag_object_sha"
                ;;
              tag)
                tag_object_json="$PRIVATE_ROOT/run-$run_id-tag-object.json"
                gh api -H "Accept: application/vnd.github+json" \
                  "/repos/$GITHUB_REPOSITORY/git/tags/$tag_object_sha" > "$tag_object_json"
                tag_commit_sha="$(jq -er '.object.sha' "$tag_object_json")"
                ;;
              *)
                echo "::error::Run $run_id tag ref resolved unexpected object type '$tag_object_type'."
                exit 1
                ;;
            esac
            if [ "$tag_commit_sha" != "$head_sha" ]; then
              echo "::error::Run $run_id RC tag does not resolve to head_sha $head_sha."
              exit 1
            fi
            printf 'refs/tags/%s\n' "$branch_name"
          }
          for index in "${!run_ids[@]}"; do
            raw="${run_ids[$index]}"
            run_id="$(echo "$raw" | xargs)"
            run_attempt="$(echo "${run_attempts[$index]}" | xargs)"
            if [[ ! "$run_id" =~ ^[1-9][0-9]*$ ]]; then
              echo "::error::Invalid load run id: $run_id"
              exit 1
            fi
            if [[ ! "$run_attempt" =~ ^[1-9][0-9]*$ ]]; then
              echo "::error::Invalid load run attempt: $run_attempt"
              exit 1
            fi
            if printf '%s\n' "${seen[@]:-}" | grep -Fxq "$run_id"; then
              echo "::error::Duplicate load run id: $run_id"
              exit 1
            fi
            seen+=("$run_id")
            destination="$PRIVATE_ROOT/load/$run_id"
            selection="$EVIDENCE_ROOT/selections/load-$run_id.json"
            # workload-load-real-azure.yml always checks out the ref it was
            # dispatched from, so a *reaffirm* load run's own head_sha is
            # whatever main/RC-tag HEAD was on the day it ran -- never the
            # historical runtime SHA it reaffirms (that SHA only lives in the
            # run's *content*, i.e. load-evidence.json's candidate.git_sha,
            # checked below against $EXPECTED_SHA). Threading the ledger SHA
            # into the artifact-selection call itself would make every
            # reaffirm load-run lookup fail-closed on a run-selection check it
            # can structurally never satisfy. Promote mode is unaffected:
            # there, $EXPECTED_SHA already equals $GITHUB_SHA, which is
            # exactly what a freshly dispatched load run's own head_sha is
            # expected to be, so run_selection_sha is unchanged from today.
            if [ "$QUALIFICATION_MODE" = reaffirm ]; then
              run_json="$PRIVATE_ROOT/load-run-$run_id.json"
              install -d -m 0700 "$PRIVATE_ROOT"
              gh api -H "Accept: application/vnd.github+json" \
                "/repos/$GITHUB_REPOSITORY/actions/runs/$run_id" > "$run_json"
              chmod 0600 "$run_json"
              run_selection_sha="$(jq -er '.head_sha' "$run_json")"
              if [[ ! "$run_selection_sha" =~ ^[0-9a-f]{40}$ ]]; then
                echo "::error::Load run $run_id does not have a resolvable head SHA."
                exit 1
              fi
              run_selection_ref="$(resolve_reaffirm_run_ref "$run_json" "$run_id" "$run_selection_sha")"
            else
              run_selection_sha="$EXPECTED_SHA"
              run_selection_ref="$GITHUB_REF"
            fi
            content="$(
              ./eng/download-qualified-run-artifact.sh \
                --repository "$GITHUB_REPOSITORY" \
                --run-id "$run_id" \
                --run-attempt "$run_attempt" \
                --workflow .github/workflows/workload-load-real-azure.yml \
                --event workflow_dispatch \
                --profile "$PROFILE" \
                --expected-sha "$run_selection_sha" \
                --expected-ref "$run_selection_ref" \
                --artifact-name "real-azure-workload-load-$PROFILE" \
                --destination "$destination" \
                --identity-output "$selection"
            )"
            mapfile -t downloaded < <(
              find "$content" -type f -name 'load-evidence.json' | sort
            )
            if [ "${#downloaded[@]}" -ne 1 ]; then
              echo "::error::Load run $run_id must contribute exactly one load-evidence.json."
              exit 1
            fi
            embedded_run_id="$(jq -r '.provenance.run_id // empty' "${downloaded[0]}")"
            if [ "$embedded_run_id" != "$run_id" ]; then
              echo "::error::Load evidence provenance run_id '$embedded_run_id' does not match selected run $run_id."
              exit 1
            fi
            embedded_attempt="$(jq -r '.provenance.run_attempt // empty' "${downloaded[0]}")"
            if [ "$embedded_attempt" != "$run_attempt" ]; then
              echo "::error::Load evidence attempt '$embedded_attempt' does not match selected attempt $run_attempt."
              exit 1
            fi
            embedded_sha="$(jq -r '.candidate.git_sha // empty' "${downloaded[0]}")"
            embedded_qualification_mode="$(jq -r '.candidate.qualification_mode // empty' "${downloaded[0]}")"
            if [ "$embedded_sha" != "$EXPECTED_SHA" ] || [ "$embedded_qualification_mode" != sealed ]; then
              echo "::error::Load evidence is not sealed evidence for the expected $QUALIFICATION_MODE SHA '$EXPECTED_SHA'."
              exit 1
            fi
            printf '%s\n' "${downloaded[0]}" >> "$evidence_paths"
            printf '%s\n' "$selection" >> "$selection_paths"
          done

          if [ "$(wc -l < "$evidence_paths")" -ne "${#seen[@]}" ]; then
            echo "::error::The immutable evidence path list is incomplete."
            exit 1
          fi
          if [ "$(wc -l < "$selection_paths")" -ne "${#seen[@]}" ]; then
            echo "::error::The immutable load selection list is incomplete."
            exit 1
          fi

      - name: Generate qualification and isolated trend
        id: generate
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$OUTPUT_ROOT"
          candidate="$(cat "$EVIDENCE_ROOT/correctness-candidate-path.txt")"
          args=(
            generate-real-azure-load-qualification
            --manifest "docs/workloads/$PROFILE.yaml"
            --candidate "$candidate"
            --policy "$POLICY_PATH"
            --correctness-selection "$EVIDENCE_ROOT/selections/correctness.json"
            --output "$OUTPUT_ROOT/$PROFILE.yaml"
            --trend-output "$OUTPUT_ROOT/$PROFILE-trend.csv"
            --run-id "$GITHUB_RUN_ID"
            --run-url "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
            --run-attempt "$GITHUB_RUN_ATTEMPT"
            --expected-sha "$EXPECTED_SHA"
            --qualification-mode "$QUALIFICATION_MODE"
          )
          exec 3< "$EVIDENCE_ROOT/load-selection-paths.txt"
          while IFS= read -r evidence && IFS= read -r selection <&3; do
            args+=(--evidence "$evidence")
            args+=(--evidence-selection "$selection")
          done < "$EVIDENCE_ROOT/load-evidence-paths.txt"
          exec 3<&-

          set +e
          dotnet exec tools/Aws2Azure.GapDocs/bin/Release/net10.0/Aws2Azure.GapDocs.dll \
            "${args[@]}"
          status=$?
          set -e
          if [ ! -f "$OUTPUT_ROOT/$PROFILE.yaml" ]; then
            status=2
            verdict=missing
          else
            verdict="$(sed -n 's/^verdict: //p' "$OUTPUT_ROOT/$PROFILE.yaml" | head -n 1)"
          fi
          {
            echo "status=$status"
            echo "verdict=$verdict"
          } >> "$GITHUB_OUTPUT"
          {
            echo "## Real-Azure workload qualification"
            echo
            echo "- Profile: \`$PROFILE\`"
            echo "- Mode: \`$QUALIFICATION_MODE\`"
            echo "- Verdict: \`${verdict:-missing}\`"
            echo "- Evidence class: production-shaped real Azure"
            echo "- Emulator baseline consumed: no"
            echo "- A/B experiment consumed: no"
            echo "- Policy: \`$POLICY_PATH\`"
            echo "- Correctness run: \`$CORRECTNESS_RUN_ID\`"
            echo "- Load runs: \`$LOAD_RUN_IDS\`"
            echo "- Expected candidate SHA: \`$EXPECTED_SHA\`"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload immutable qualification bundle
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: real-azure-workload-qualification-${{ inputs.profile }}
          path: |
            artifacts/qualification-output/**
            artifacts/qualification-input/selections/**
          if-no-files-found: error
          retention-days: 90

      - name: Enforce qualified verdict
        if: always()
        shell: bash
        run: |
          if [ "${{ steps.generate.outputs.status }}" != "0" ] ||
             [ "${{ steps.generate.outputs.verdict }}" != "qualified" ]; then
            echo "::error::Qualification did not produce a qualified verdict. Investigate the evidence; do not raise thresholds or rerun blindly."
            exit 1
          fi

      - name: Remove downloaded source evidence
        if: always()
        run: rm -rf "$PRIVATE_ROOT"
